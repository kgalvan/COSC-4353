package characterprocessor;

public class CharacterProcessor {
    public String upperCaseConverter(String characters) { //Feedback: text instead of characters
        return characters.toUpperCase();
    }
}
